# ft_irc テスト手順ガイド

このドキュメントは、`Eval_IRC_2022.pdf` の各要件をテストするための具体的なコマンドと手順をまとめたものです。

**テストの準備:**

1.  **サーバーのコンパイル:**
    ```bash
    make
    ```

2.  **サーバーの起動:**
    ```bash
    ./ircserv <ポート番号> <パスワード>
    ```
    例:
    ```bash
    ./ircserv 6667 password
    ```

## 必須課題 (Mandatory Part)

### 基本チェック

- **[ ] `Makefile` が存在し、プロジェクトが正しくコンパイルされ、C++で書かれていること。実行ファイル名は `"ircserv"` であること。**
    - **手順:**
        1.  `make` を実行し、コンパイルエラーや警告が出ないことを確認します。
        2.  `ls` を実行し、`ircserv` という名前の実行ファイルが生成されていることを確認します。

- **[ ] コード内に `poll()` (または同等のもの) が**1つだけ**存在すること。**
    - **手順:** プロジェクトのソースコード全体で `poll` というキーワードを検索します。
    - **コマンド例:**
        ```bash
        grep -r "poll(" srcs/
        ```
    - **確認:** `Server.cpp` のメインループ内に1回だけ呼び出しがあることを確認します。

- **[ ] `poll()` (または同等のもの) が、`accept`、`read/recv`、`write/send` の各呼び出しの前に常に実行されていること。**
    - **手順:** `Server.cpp` のソースコードを目視で確認します。
    - **確認:** `handleNewConnection`, `handleClientData`, `handleClientSend` などの関数が `poll()` の結果に基づいて呼び出されていることを確認します。`recv` や `send` の呼び出しが、`poll` イベント (`POLLIN`, `POLLOUT`) のチェックブロック内にあることを確認します。

- **[ ] これらの呼び出しの後、`errno == EAGAIN` の場合に読み取りを再試行するなどの、特定の動作をトリガーするために `errno` を使用しないこと。**
    - **手順:** `handleClientData` と `handleClientSend` の実装を確認します。
    - **確認:** `recv` や `send` が `-1` を返した場合、`errno` が `EAGAIN` または `EWOULDBLOCK` であれば、ループで再試行するのではなく、即座に **return** して次の `poll` を待つ実装になっていることを確認します。

- **[ ] `fcntl()` の各呼び出しは、`fcntl(fd, F_SETFL, O_NONBLOCK);` の形式でのみ行われること。その他の `fcntl()` の使用は禁止。**
    - **手順:** プロジェクトのソースコード全体で `fcntl` を検索します。
    - **コマンド例:**
        ```bash
        grep -r "fcntl(" srcs/
        ```
    - **確認:** `Server.cpp` の `setNonBlocking` 関数内でのみ、指定された形式で使用されていることを確認します。

---

### ネットワーク機能

- **[ ] サーバーが起動し、コマンドラインで指定されたポートで全てのネットワークインターフェースをリッスンしていること。**
    - **手順:** サーバーを起動した後、別のターミナルで `netstat` や `lsof` コマンドを実行します。
    - **コマンド例 (ポート 6667 の場合):**
        ```bash
        netstat -an | grep 6667
        # または
        lsof -i :6667
        ```
    - **確認:** 指定したポートが `LISTEN` 状態になっていることを確認します。

- **[ ] `nc` ツールを使用して、サーバーに接続し、コマンドを送信すると、サーバーが応答すること。**
    - **手順:** `nc` (netcat) を使ってサーバーに接続し、`PING` などの簡単なコマンドを送信します。
    - **コマンド:**
        ```bash
        nc localhost 6667
        PING hello
        ```
    - **期待される応答:**
        ```
        :localhost PONG localhost :hello
        ```

- **[ ] 参照IRCクライアントを使用して、サーバーに接続できること。**
    - **手順:** `irssi` などのIRCクライアントを使用して接続します。
    - **コマンド (irssi の場合):**
        ```
        /connect localhost 6667 <パスワード>
        ```
    - **確認:** `Welcome to the Internet Relay Network` のようなウェルカムメッセージが表示され、正常に接続できることを確認します。

- **[ ] サーバーが複数のクライアント接続を同時に処理し、ブロックしないこと。**
    - **手順:** 2つ以上のターミナルを開き、それぞれで `nc` や `irssi` を使ってサーバーに接続します。
    - **確認:** 一方のクライアントでメッセージを送信している間も、もう一方のクライアントがコマンドを送信でき、サーバーが応答することを確認します。

- **[ ] あるクライアントからチャンネルに送信された全てのメッセージが、そのチャンネルに参加している他の全てのクライアントに転送されること。**
    - **手順:**
        1.  **クライアントA** と **クライアントB** がサーバーに接続し、認証を済ませます。
        2.  両方のクライアントが同じチャンネルに参加します: `/join #test`
        3.  **クライアントA** がチャンネルにメッセージを送信します: `PRIVMSG #test :Hello everyone!`
    - **確認:** **クライアントB** の画面に、クライアントAからのメッセージが表示されることを確認します。クライアントAには表示されないのが一般的です。

---

### ネットワークに関する特殊事項

- **[ ] `nc` を使用して部分的なコマンドを送信しても、サーバーが正しく応答すること。**
    - **手順:** `nc` で `Ctrl+D` を使い、コマンドを分割して送信します。
    - **コマンド:**
        ```bash
        nc localhost 6667
        PRI<Ctrl+D>VMSG #test :Hel<Ctrl+D>lo<Enter>
        ```
    - **確認:** サーバーが "PRIVMSG #test :Hello" という1つのコマンドとして正しく解釈し、処理することを確認します。

- **[ ] クライアントが予期せず切断されても、サーバーが他の接続や新規接続に対して引き続き動作すること。**
    - **手順:**
        1.  3つのクライアント（A, B, C）を接続します。
        2.  クライアントBのプロセスを `kill -9` で強制終了します。
        3.  クライアントAがメッセージを送信できるか確認します。
        4.  クライアントCが新規に接続できるか確認します。
    - **確認:** サーバーがクラッシュせず、他のクライアントの動作に影響がないことを確認します。

- **[ ] `nc` でコマンドを途中で送信した状態で予期せず切断しても、サーバーが異常な状態になったりブロックされたりしないこと。**
    - **手順:**
        1.  `nc` で接続し、`PASS mypass` のように入力しますが、Enterキーは押しません。
        2.  `nc` のプロセスを強制終了します。
    - **確認:** サーバーがハングしたり、CPU使用率が異常に高くなったりしないことを確認します。

- **[ ] チャンネルに接続しているクライアント (`Ctrl+Z` で停止) がいる状態で、別のクライアントがチャンネルに大量のメッセージを送信しても、サーバーがハングしないこと。**
    - **手順:**
        1.  クライアントAとBが `#test` チャンネルに参加します。
        2.  クライアントBのターミナルで `Ctrl+Z` を押し、プロセスを一時停止します。
        3.  クライアントAから `#test` に大量のメッセージを送信します。
        4.  クライアントBのターミナルで `fg` を実行し、プロセスを再開します。
    - **確認:** サーバーがハングせず、クライアントBが再開したときに溜まっていたメッセージを全て受信することを確認します。また、`valgrind` などでメモリリークがないかも確認します。

---

### クライアントコマンド

- **[ ] `nc` と通常のIRCクライアントの両方で、認証、ニックネーム設定、ユーザー名設定、チャンネル参加ができること。**
    - **手順 (nc):**
        ```
        PASS <password>
        NICK mynick
        USER myuser 0 * :Real Name
        JOIN #test
        ```
    - **手順 (irssi):**
        ```
        /connect localhost 6667 <password>
        /nick mynick
        /join #test
        ```
    - **確認:** いずれの方法でもエラーなく一連の操作が完了することを確認します。

- **[ ] プライベートメッセージ (`PRIVMSG`) および通知 (`NOTICE`) が、様々なパラメータで完全に機能すること。**
    - **手順:**
        1.  クライアントAからクライアントBへ `PRIVMSG` を送信: `/msg bob Hello`
        2.  クライアントAからチャンネル `#test` へ `PRIVMSG` を送信: `メッセージを直接入力`
        3.  上記を `NOTICE` コマンドでも試す: `/notice bob Hello`
    - **確認:** ユーザー宛、チャンネル宛のメッセージが正しく配信されること。`NOTICE` でエラーが発生しても、送信元に応答が返らないことを確認します。

- **[ ] 一般ユーザーがオペレーターアクションを実行する権限を持たないこと。**
    - **手順:**
        1.  クライアントAとBがチャンネルに参加します（Aがオペレーター）。
        2.  クライアントB（一般ユーザー）がオペレーターコマンドを試します。
        - `/kick alice`
        - `/topic New Topic` (mode +t の場合)
        - `/mode #test +i`
        - `/invite charlie #test`
    - **確認:** `ERR_CHANOPRIVSNEEDED (482)` などの適切なエラーメッセージが返され、コマンドが失敗することを確認します。

- **[ ] 全てのチャンネル操作コマンドがオペレーター権限を持つユーザーでテストされること。**
    - **手順:** オペレーターであるクライアントAが以下のコマンドを実行します。
        - `KICK`: `/kick bob`
        - `INVITE`: `/invite charlie #test`
        - `TOPIC`: `/topic A new topic`
        - `MODE`:
            - `/mode #test +i` (招待制)
            - `/mode #test +t` (トピック制限)
            - `/mode #test +k mykey` (キー設定)
            - `/mode #test +o charlie` (オペレーター権限付与)
            - `/mode #test +l 10` (人数制限)
    - **確認:** 各コマンドが仕様通りに動作し、チャンネルの状態が変更され、その旨がチャンネルメンバーに通知されることを確認します。

## ボーナス課題

- **[ ] ファイル転送**: (実装されている場合)
    - DCC (Direct Client-to-Client) プロトコルは IRC のコア仕様ではないため、通常 `PRIVMSG` を介してネゴシエーションされます。
    - **手順例 (irssi):** `/dcc send <nick> <file>`
    - **確認:** サーバーがDCC関連の `PRIVMSG` をブロックせず、クライアント間の直接通信が確立されることを確認します。サーバー自体がファイル転送を中継する必要はありません。

- **[ ] 小さなボット**: (実装されている場合)
    - **手順:** ボットが応答するように設計された特定のコマンドやメッセージを送信します。
    - **例:** チャンネルで `!help` や `!time` と発言する。
    - **確認:** ボットが正しく応答メッセージをチャンネルやユーザーに送信することを確認します。
